name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  game-logic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: game-logic.js のユニットテストを実行
        run: node --test
